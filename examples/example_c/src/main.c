
// A program that parses a command line arguments string
// and prints it out in json format

int main(int argc, char* argv[]) {
	// call the parser on the input variable
	// call the json serializer on the result
	// print the json string
	return 0;
}
