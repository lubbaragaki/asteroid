// define a json-like data structure to store
// the command line arguments (like a hashmap?)

// function that takes a hashmap and a string
// like "--opt=val" and adds a new element to
// the hashmap with 'opt' as key and 'val' as
// value

// function that splits a string into a list of
// string based on a delimiter

// main procedure that uses all of the above
// to deserialize the command line arguments
