// function that returns a serialized string
// representing a key value pair like: 
// "key": "value" (or whatever type the value is)

// function that uses the one above to iteratively
// (and recursively if the hashmaps are nested)
// construct a string of the json

